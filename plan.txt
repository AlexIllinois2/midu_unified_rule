"~/Downloads/【离线】 字节跳动/bytedance/midu/zh-cn/frontend/g2p/mnc/unified_rule.txt" 是multitts语音包的发音补丁 内容比较混乱 帮我整理一下吧: 去重, 分组和排序.

去重: 一样的行, 或内容完全等价

分组: 词边界规则, 拼音模式规则, 上下文规则等. 用注释和空行分隔

排序: 
- 分组排序：按条数升序 → PINYIN 2 → WORD_INSIDE 22 → TEXT 1205（不变）
- 组内排序：
	- 一级：按第1列字数（单字在前，多字词在后）
	- 二级：第2列为 * 非拼音标记的行排在前
	- 三级：按拼音——* 行用第1列汉字转拼音(带数字声调，轻声补5)，普通行用第2列原值


帮我写个简单的html页面, 用于生成这种规则内容:
表单定义: key(中文字词), 读音(*5这种或renr4 NONE, tangr4 none5, xiao6 man3等形式的拼音), 匹配规则(PINYIN(), WORD_INSIDE(), TEXT()等. 默认TEXT()), 匹配值(如: (吧)台, 初始值放个"()"), 注释(默认空)
页面操作: 添加按钮(点添加后生成一行规则字符串放在下方内容区, 同时重置表单状态等待填写新一条规则), 复制按钮((如果表单内容已经填写完整未提交, 自动添加内容,)将所有已生成的规则复制到剪切板)

